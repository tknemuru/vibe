Task1は成果物OKだが、次タスクへ進む前に作業ツリーをクリーンにする（必須ゲート）。

1) git status --porcelain を提示
2) Task1スコープ外の変更をすべて作業ツリーから排除する:
   - config/jobs.yaml: 変更をrevert（git checkout -- config/jobs.yaml など）
   - data/app.db*: 追跡対象ならrevert、追跡対象外なら.gitignoreへ（ただしTask2ではなく別PR推奨）
   - docs/data/: 追跡対象外のバックアップなら削除 or git cleanで除去（repoに残さない）
3) 最終的に git status --porcelain が
   - docs/migration-cli-rename.md の追加
   - ystemctl... の削除
   以外 0 になる状態にする（もしくは Task1 はすでにコミットして他は未ステージ・破棄でも良いが、作業ツリーはクリーンにする）

完了報告：
- git status --porcelain（空になった証跡）
- Task1の変更ファイル一覧（2件のみ）再掲
