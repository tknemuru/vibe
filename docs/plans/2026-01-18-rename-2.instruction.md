# Task2: CLI entry を dre に置換（後方互換なし）

1) package.json を更新:
   - name: "vibe" -> "dre"（全面移行方針）
   - bin: { "vibe": "./dist/cli.js" } -> { "dre": "./dist/cli.js" }
   - vibe bin が残らないこと

2) src/cli.ts を更新:
   - .name("vibe") -> .name("dre")
   - --help/usage に直結する vibe 表記があれば最小限で修正

3) Verify:
   - npm run build（あれば）
   - node ./dist/cli.js --help（ビルド後）
   - node -e "console.log(require('./package.json').bin)" で dre のみ確認
   - rg で bin定義に vibe が残っていないこと確認

4) 変更ファイル一覧、Verify結果を報告。
注意: Task2では docs の一括置換や Vibe Copy Server 等のブランド文言一掃はやらない（Task3で実施）。
