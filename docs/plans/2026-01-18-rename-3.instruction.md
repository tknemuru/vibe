# Task3: repo内の "dre"/"DRE" 文言を完全撲滅（0件）

1) Task1の migration doc を参照し、対象を順に潰す:
   - src/** のログ/メッセージ/HTML/メール文言を dre/DRE に統一
   - test/** の期待値を更新
   - config/jobs.yaml の claude-dre-coding を dre 系へ更新
   - docs/** を dre に更新（setup/ops/windows-task-scheduler/plans）

2) Verify:
   - npm run build
   - npm test（存在すれば）
   - node ./dist/cli.js --help
   - rg -n "\bdre\b" .  -> 0件
   - rg -n "DRE" .      -> 0件

3) 報告:
   - 変更ファイル一覧
   - Verify結果
   - rg 0件の証跡
