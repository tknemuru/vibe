defaults:
  interval: 3h
  mail_limit: 5
  max_per_run: 20
  fallback_limit: 3
  limit: 5
  freshness: Week
  allowlist:
    - note.com
    - zenn.dev
    - qiita.com
jobs:
  - name: consultant-books
    queries:
      - コンサルタント 仕事
    enabled: true
